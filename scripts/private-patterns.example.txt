# Copy to scripts/private-patterns.txt and fill in. That file is gitignored.
#
# One extended-regex per line; blank lines and # comments are ignored. These
# are the identifiers that must never reach a public repository but cannot be
# written into the committed gate script, because the script is published too.
#
# In CI these come from the EXTRA_SECRET_PATTERNS repository secret instead,
# as the same newline-separated list.
#
# Typical entries:
#   your-domain-name
#   YourPoolName|YourOtherPool
#   3009[0-9]
