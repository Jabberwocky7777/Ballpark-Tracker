# Copy to scripts/private-patterns.txt and fill in. That file is gitignored.
#
# These are the identifiers that must never reach a public repository, and that
# cannot be written into scripts/check-no-secrets.sh, because that script is
# published too and scans itself.
#
# One extended-regex per line. Blank lines and # comments are ignored.
# In CI the same list comes from the EXTRA_SECRET_PATTERNS repository secret.
#
# Typical entries:
#
#   your-domain-name
#   YourPoolName|YourOtherPool
#   \b3[0-9]{4}\b
#   \b(firstname|othername)\b
